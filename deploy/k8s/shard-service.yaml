apiVersion: v1
kind: Service
metadata:
  name: shard
  namespace: distributed-search
spec:
  clusterIP: None
  selector:
    app: shard
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
    - name: metrics
      port: 2113
      targetPort: 2113
