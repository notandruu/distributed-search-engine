# Benchmarks

Run `make bench` to execute the ghz load test against a running cluster.

Run `make bench-summary` to generate `BENCHMARK_SUMMARY.md` from the raw JSON output.

See [docs/BENCHMARKS.md](../docs/BENCHMARKS.md) for results and methodology.
